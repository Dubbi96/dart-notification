# 앱 변경 기록 (오너 확인용)

> **용도**: 반영(배포·EAS 업데이트)마다 "어느 화면의 무엇이 바뀌었나 + 앱에서 어떻게 확인하나"를 기록한다.
> 오너가 앱을 열고 이 문서와 대조하며 판단하는 것이 목적. **매 반영 시 PM(Claude)이 갱신** — 최신이 맨 위.
> 전제: 폰에 **v1.0.3(4) 신규 APK** 설치 필요(runtimeVersion `1.0.3`). 설치 후 같은 `play` 채널의 JS 업데이트를 수신한다.

---

## 2026-07-30 (목) 배치 #19 — 에디션 판단 카드 디자인 정밀화 (DAR-570)

**배경**: 배치 #18의 종합 의견·투자 플랜을 320/360px 좁은 화면, 긴 회사명·긴 판단 문구, 1.5배 글자 크기로 압박 검토했다. 기존 첫 카드는 정보가 모두 펼쳐져 360px에서 약 618px까지 늘어났고, 회사명과 상태 배지가 폭을 다투며, 아직 진입할 수 없는 종목에도 +10%/-5% 수치가 보여 행동 우선순위가 흐려졌다.

**모바일**

| 화면 | 무엇이 바뀌었나 | 확인 방법 |
|---|---|---|
| **에디션 종합 의견** | 큰 숫자 3열을 작은 상태 지표로 압축하고, 헤드라인을 `N개만 조건부 진입 검토` 또는 `지금은 진입보다 확인이 먼저예요`로 단순화. 우선 판단은 별도 박스에 유지 | 신호 탭 → 회사 카드보다 먼저 전체/조건부 검토/대기/리스크 지표와 한 문장 결론이 읽히는지 확인 |
| **종목 카드 헤더** | 회사명과 상태 배지를 같은 행에서 분리. 회사명 최대 2줄, 이벤트·등급·점수 한 줄 말줄임, 상태는 다음 행에 독립 배치 | 긴 회사명 카드에서 상태 배지·점수·화살표가 겹치거나 밀리지 않는지 확인 |
| **판단·행동 위계** | 판단 근거와 다음 확인 행동을 각각 최대 2줄로 제한하고, 미충족 조건의 중복 표기를 제거. 대기/리스크 카드는 `먼저 확인할 것`과 진입 보류 이유만 표시 | 조건 미충족/리스크 카드에서 같은 문장이 반복되지 않고, 무엇을 먼저 확인할지 한 번에 읽히는지 확인 |
| **조건부 단기 기준** | +10% 청산 참고·-5% 중단·최대 5거래일을 **조건 충족·리스크 없음·점수 40 이상 카드에만** 3열로 표시. 대기/리스크 카드에서는 수치를 숨김 | 준비 카드에는 3개 수치가 한 줄 정렬되고, 대기/리스크 카드에는 단타 수치가 없는지 확인 |
| **릴리스 식별** | `1.0.2(3)` → **`1.0.3(4)`**, runtimeVersion도 `1.0.3`으로 분리 | 설치 후 Android 앱 정보에서 버전 확인 |

**시각 검증**

- 360px 긴 문구 fixture: 첫 카드 `618px → 405px`(약 34% 감소), 대기·리스크 카드 `320px`.
- 320px 최소 폭: 가로 오버플로 0, 단기 기준 3개 고정 3열 유지.
- 1.5배 글자: 회사명 2줄 확장, 메타 1줄 말줄임, 카드/배지 충돌 0.
- `check-edition-decision-density.ts`를 품질 게이트에 추가해 헤더 분리·최대 줄 수·상태별 단기 수치 노출·3열 정렬을 결정론적으로 고정.

**백엔드·DB**: 무변경. OCI 프로덕션 배포와 DB 마이그레이션도 실행하지 않음.

---

## 2026-07-30 (목) 배치 #18 — 19시 에디션 종합 의견·조건부 투자 플랜 (DAR-570)

**배경**: 기존 19:05 투자판단 에디션은 회사/공시 카드부터 바로 나열해 “그래서 무엇이 좋고, 무엇을 확인해야 하며, 언제 계획을 멈춰야 하는가”가 연결되지 않았다. 오너 피드백에 따라 정보 순서를 `종합 의견 → 종목별 판단 → 근거 → 진입 조건 → 중단 조건 → 단기 참고 시나리오`로 재구성했다.

**모바일**
| 화면 | 무엇이 바뀌었나 | 확인 방법 |
|---|---|---|
| **신호 탭 · 매수 에디션 상단** | **‘오늘의 종합 의견’ 카드** 신설 — 전체 판단 수·조건 준비 수·리스크 수와 먼저 볼 1순위 판단을 한 화면에서 요약. 과거 에디션은 ‘당시의 종합 의견’으로 시점을 명시 | 매수 신호가 있는 에디션 진입 → 회사 카드보다 먼저 종합 의견과 우선순위가 보이는지 확인 |
| **에디션 종목 카드** | 단순 회사/공시 카드 대신 **‘조건부 진입 검토 / 조건 확인 전 대기 / 리스크 확인 전 대기 / 근거 상세 확인’** 판단을 먼저 제시. 이어서 한 줄 판단·진입 전 확인·계획 중단 기준을 같은 카드 안에 배치 | 조건 충족/미충족 또는 리스크가 다른 에디션 카드에서 판단 라벨과 확인 항목이 함께 바뀌는지 확인 |
| **에디션 종목 카드 · 단기 참고 시나리오** | 백엔드 정본 `short-momentum` 규칙과 동기화된 **다음 진입 가능일 시가·체결가 +10% 청산 참고·-5% 중단·최대 5거래일**을 노출. 19시에는 미래 체결가를 알 수 없으므로 근거 없는 절대 원화 목표가는 만들지 않고, 실제 체결가 기준임을 명시 | 점수 40 이상 카드 → ‘단기 참고 시나리오’의 4개 기준과 ‘조건 충족 시에만’ 고지 확인 |
| **릴리스 식별** | 앱 버전 `1.0.1(2)` → **`1.0.2(3)`**, runtimeVersion도 `1.0.2`로 분리 | 설치 후 Android 앱 정보에서 버전 확인 |

**안전·정합 가드**
- 서버가 내려준 매수점수·필수 진입조건·리스크만 조합하며 점수나 조건을 모바일에서 재계산하지 않는다.
- 단기 규칙 값은 Jest가 백엔드 `strategy-presets.ts`의 `short-momentum` 정본과 직접 대조해 드리프트를 차단한다.
- 단기 시나리오는 실제 체결가 기준 참고 규칙이고, 필수 조건이 깨지면 적용하지 않는다는 맥락 고지를 기존 투자자문 아님 면책 위에 추가했다.

**백엔드·DB**: 무변경(기존 일일 에디션 응답과 전략 룰북을 모바일 표시층에서 정직하게 조합, 마이그레이션 0).

**APK 산출물**

- EAS build: `dfa547e5-3498-4863-8dee-fc2f4113d530` — `FINISHED`, profile `play-apk`, channel `play`, source commit `f83c87ce6`
- 다운로드: <https://expo.dev/artifacts/eas/Tuol2KdoiF7H_GxoLM_LvXmAcXRjIgpT4rrnQci3_YM.apk>
- 로컬: `/Users/gangjong-won/Dubbi/releases/gongsion-v1.0.2-build3.apk`
- 크기: `118,909,408 bytes` · SHA-256: `afc63ae271a00c681ad8737c5bd692aa99f59bbfa2a25743343de009a0a54404`
- ZIP/APK 무결성: 1,406 entries 전수 `unzip -t` 통과 · ABI `arm64-v8a`, `armeabi-v7a`, `x86`, `x86_64`
- EAS 메타데이터: app version `1.0.2`, build version `3`

---

## 2026-07-27 (일) 배치 #17 — v1.0.1 릴리스 견고화·Android 브랜드 자산 수정 (DAR-569)

**배경**: 최신 `origin/main`을 다음 확인용 APK 기준으로 전수 감사했다. `feat/gap-deploy-script`는 main보다 67커밋 뒤인 서버 배포 스크립트 작업이라 앱 릴리스 기반에서 제외했다. 모바일은 기능 회귀 게이트는 통과했지만 Expo SDK 호환성, standalone 필수 peer dependency, Android adaptive/themed icon 설정, 이벤트 라벨 전수성, 실행되지 않던 결정론 검사와 Jest open handle이 릴리스 위험으로 남아 있었다.

**모바일 (새 APK 설치 필요 — v1.0.1, versionCode 2)**

| 영역 | 무엇이 바뀌었나 | 확인 방법 |
|---|---|---|
| **Android 앱 아이콘** | Expo 템플릿 가이드가 든 `backgroundImage`와 Expo 기본 심볼인 `monochromeImage` 참조 제거. 브랜드 foreground + teal 배경색만 사용해 일반/테마 아이콘의 타 브랜드·가이드 노출 차단 | 설치 후 런처에서 일반 아이콘 확인. Android 테마 아이콘을 켜도 Expo 꺾쇠 심볼이 나타나지 않는지 확인 |
| **시작 화면** | 구형 `splash` 필드를 SDK 56 `expo-splash-screen` 플러그인으로 이관. 라이트 teal·다크 navy 배경 명시 | 앱 강제 종료 후 재실행 → 시작 화면이 스키마 오류 없이 브랜드 로고로 표시되는지 확인 |
| **이벤트 라벨** | 행정·규제 절차, 기업가치 제고 계획, 결산실적 발표 예고, 자율 경영공시 4종을 구체적인 한국어 라벨로 추가 | 해당 유형의 공시/통계 카드에서 영문 enum이나 뭉뚱그린 “기타” 대신 한국어 라벨 확인 |
| **런타임 안정성** | Expo SDK 56 호환 패치 11종 반영, standalone 필수 `expo-font` 추가, Axios 1.18.1 보안 패치 | `expo-doctor` 21/21 및 Android 번들 통과 |
| **릴리스 식별** | 앱 버전 `1.0.0(1)` → `1.0.1(2)`, runtimeVersion도 `1.0.1` 경계로 분리 | 설치 후 앱 정보에서 버전 확인 |
| **릴리스 CI 공급망** | 당일 새로 탐지된 백엔드 ZIP·tar·YAML·glob DoS 경로를 실제 의존성 패치로 제거. API/DB 코드는 변경하지 않음 | 백엔드 build + 343 suites/4,615 tests, 보안 게이트 critical 0 통과 |

**품질 기법**

- 기존 `mobile/scripts/check-*.ts` 134개를 일괄 실행하는 `npm run quality:checks`를 추가하고 PR CI 하드 게이트에 편입했다.
- 코드 형태가 개선된 뒤 낡은 정규식 때문에 실패하던 검사 5개를 현재 구조와 동기화했다.
- 테스트 QueryClient의 GC 타이머와 Expo 아이콘 비동기 폰트 로드를 테스트 더블로 격리하고 CI 테스트에 종료 상한을 둬, 결과 출력 뒤 잡이 멈추는 문제를 차단했다.
- 보안 게이트 재실측 중 새로 차단된 `adm-zip`, `brace-expansion`, `js-yaml`, `tar` advisory는 allowlist에 추가하지 않고 `adm-zip 0.6.0`, `bcrypt 6.0.0`, 호환 override로 제거했다. Axios·`fast-xml-parser`도 함께 패치했다.

**범위 경계**: 백엔드는 의존성/lockfile만 변경했고 API·도메인·DB 코드는 바꾸지 않았다. OCI 프로덕션 배포와 DB 마이그레이션은 실행하지 않았다. 알림탭 seen 배선은 별도 PR #548(DAR-568)이며 이 APK 소스에는 포함하지 않는다.

**APK 산출물**

- EAS build: `d4df8e05-6bda-4d83-a53c-b5f44164d2f2` — `FINISHED`, profile `play-apk`, channel `play`, source commit `b49c07220`
- 다운로드: <https://expo.dev/artifacts/eas/VihT3m8bjhsLya9VkZgzQQRSLj0QYm2l5Zy0IHSCy7E.apk>
- 크기: `118,886,472 bytes` · SHA-256: `ba24da118e2d497cccd93c23e0bbf4b7c9e16023a9a8e5017d29458febe096d0`
- ZIP/APK 무결성: 1,406 entries 전수 `unzip -t` 통과 · ABI `arm64-v8a`, `armeabi-v7a`, `x86`, `x86_64`

---

## 2026-07-18 (토) 배치 #15 — 로그아웃 등 확인 다이얼로그 백드롭 풀스크린 수정 (DAR-561)

**배경**: 오너 실기기 스크린샷 트리아지(`cc-apk-feedback-triage-2026-07-18.md` §1-5a) — 로그아웃 확인 다이얼로그의 백드롭(어둡게 깔리는 배경)이 화면 전체를 덮지 못하고 탭바·헤더 위로 뚫려 보였다. 원인: 다이얼로그가 Paper `<Portal><Dialog>`(JS 절대배치 오버레이)로 구현돼 있어, Android(Fabric)에서 탭바·헤더의 네이티브 elevation이 백드롭보다 위로 올라오는 z-order 역전이 발생.

**수정**: `DialogProvider.tsx` 내부 렌더를 RN 코어 `<Modal transparent statusBarTranslucent navigationBarTranslucent>` + 백드롭(`Pressable`, 탭하면 닫힘)으로 전면 교체. 아이콘·타이틀·버튼 마크업과 `useDialog()`/`getDialogRef()` 공개 API는 무변경(소비처 8곳 수정 0). 신규 디자인 룰 **R-22**(풀스크린 백드롭은 RN 코어 Modal만) 추가 + ESLint `no-restricted-imports`로 Paper `Dialog`/`Portal` 임포트를 전면 차단(재발 방지).

**모바일 (EAS 업데이트, 앱 재시작 2회 후 반영)**
| 화면 | 무엇이 바뀌었나 | 확인 방법 |
|---|---|---|
| **설정 탭 · 로그아웃 확인 다이얼로그**(및 `showDialog()`를 쓰는 모든 확인창 — 회원가입 안내·관심기업 삭제 확인 등) | 백드롭이 탭바·헤더까지 포함해 화면 전체를 고르게 어둡게 덮음(이전엔 하단 탭바·상단 헤더가 백드롭 위로 뚫려 보임) | 설정 탭 → 로그아웃 탭 → 확인 다이얼로그가 뜰 때 상단 헤더·하단 탭바까지 함께 어두워지는지 확인. 백드롭 탭 시 다이얼로그 닫힘도 동일 |

**백엔드**: 무변경 (mobile/ 한정 UI 렌더 교체. `useDialog` API·소비처 로직 무접촉).
## 2026-07-18 (토) 배치 #14 — 알림 뱃지 seen/read 이원화, 백엔드 API 선행 (DAR-563)

**배경**: 오너 실기기 리포트 — 알림 뱃지가 "읽음 처리"(행 탭·전체읽음)로만 줄어들어, 탭을 열어보기만 해도 사라져야 할 "99+"가 영구 누적됐다.

**백엔드 (API 계약 준비 완료, 화면 변화는 모바일 소비 착수 후)**
| 무엇이 바뀌었나 | 확인 방법 |
|---|---|
| `POST /notifications/seen` 신규 — 호출 시 "알림탭 마지막 방문 시각"을 갱신 | `curl -X POST .../notifications/seen` → `notificationsLastSeenAt` 갱신 확인 |
| `GET /notifications`의 뱃지(`unreadCount`/`unreadByType`/`unreadByCategory`)가 "읽음 여부"가 아니라 "마지막 방문 이후 신규 도착" 기준으로 재계산 | seen 호출 후 뱃지가 0으로, 신규 알림 도착 시 다시 올라가는지 |
| 행별 읽음(isRead)·개별/전체 읽음 처리는 완전히 그대로 — 탭한 알림의 미읽음 하이라이트는 유지됨(뱃지만 별도) | 알림탭 진입 후에도 안 읽은 행의 강조 표시가 남아있는지 |

**모바일**: 아직 미반영 — 알림탭 진입 시 위 `seen` API를 호출하는 소비 로직은 후속 반영 예정(현재는 백엔드 계약만 준비된 상태라 뱃지 동작은 이전과 동일).

---

## 2026-07-18 (토) 배치 #13 — 로딩 견고화: 스켈레톤 무기한 대기·기업상세 데드엔드·연결실패 카피 정리 (DAR-560)

**배경**: 오너 실기기 리포트 — 종목 상세가 실패해도 ~33초 무피드백에 이어 재시도 버튼 없는 "기업 정보를 찾을 수 없습니다" 화면으로 낙하(네트워크 순간 오탐 시 무기한 재현). 연결 실패 안내엔 "같은 Wi-Fi에…"·API 주소 등 개발용 문구가 그대로 노출.

**모바일 (EAS 업데이트, 앱 재시작 2회 후 반영)**
| 화면 | 무엇이 바뀌었나 | 확인 방법 |
|---|---|---|
| 상세화면 전반(공시·신호·종목상세·포지션·재무·판단허브 등) | 로딩 스켈레톤이 10초 넘게 이어지면 "지연되고 있어요" 재시도 안내로 자동 전환(무기한 로딩 제거) | 네트워크를 느리게 만들고 해당 화면 진입 → 10초 뒤 재시도 버튼 노출 확인 |
| **종목 상세** | 네트워크 순간 오탐 등으로 조회가 멈춰도 재시도 없는 "기업 정보를 찾을 수 없습니다" 데드엔드 대신 "네트워크 대기 중 · 다시 시도" 안내로 전환. '미존재' 문구는 서버가 실제 404를 준 경우에만 노출 | 기내모드 토글 직후 종목 상세 진입 → 데드엔드 대신 재시도 버튼 확인 |
| 연결 실패 안내(공통, `ApiErrorState`) | "같은 Wi-Fi에…"·API 주소 등 개발용 문구를 프로덕션에서 제거 — "일시적으로 연결할 수 없어요 / 네트워크 상태를 확인한 뒤 다시 시도해 주세요"로 교체(개발 빌드는 기존 진단 문구 그대로 유지) | 서버 의존 화면에서 연결 실패 재현 → 안내 문구 확인 |

**백엔드**
| 무엇이 바뀌었나 | 확인 방법 |
|---|---|
| 종목 상세 조회 시 기업개황 캐시가 없거나 24시간 넘게 지났어도 DART 응답을 기다리지 않고 즉답(최신 정보는 백그라운드에서 갱신 후 다음 조회부터 반영) — 서버 DART 호출(최대 30s)이 클라 타임아웃(10s)보다 길어 항상 실패하던 역전 제거 | DART 응답이 느릴 때도 종목 상세 첫 조회가 즉시 오는지 |
## 2026-07-18 (토) 배치 #12 — Play 소비자 빌드 표면 게이팅 확장 + Pro 카피 정직화 (DAR-558)

**배경**: 오너 실기기 스크린샷 트리아지(`cc-apk-feedback-triage-2026-07-18.md`) — DAR-549 게이팅은 트레이딩 4표면만 커버해 Pro 업셀·내부 운영(ops) 화면·매도 세그먼트가 첫 Play 빌드에 그대로 노출됐다. Pro 혜택 4개 중 3개는 구현 전무, '무제한'은 실구현(PRO=200종목)과 불일치해 Google Play Misleading Claims 리스크가 있었다.

**구현 방식**: DAR-549와 동일한 빌드타임 플래그 패턴 2종 신설 — `EXPO_PUBLIC_SHOW_PRO_UPSELL`·`EXPO_PUBLIC_SHOW_OPS`(둘 다 기본 `true`, `play`/`play-apk` 프로파일에서만 `false`). **기존 oci/preview/production 채널은 두 플래그 모두 무변경**(회귀 0). 매도 세그먼트는 기존 `EXPO_PUBLIC_SHOW_TRADING`으로 게이팅.

**모바일 — `play` 빌드에서만 (oci 빌드는 노출 무변경)**
| 화면 | 무엇이 바뀌었나 | 확인 방법 (play 빌드 기준) |
|---|---|---|
| **설정 탭** | 'Pro로 업그레이드' 배너 미노출 | 로그인 상태에서 설정 탭 상단에 Pro 배너가 없는지 |
| **설정 탭** | '수집 현황'·'AI 비용/거버넌스' 메뉴 항목 미노출 | 설정 탭 '일반' 섹션에 두 항목이 없는지 |
| **Pro/AI비용/수집현황 라우트** | 딥링크·잔존 링크로 직접 진입 시 **설정 탭으로 리다이렉트** | 구 링크로 `/settings-detail/pro`, `/settings-detail/ai-cost`, `/settings-detail/collection-status` 진입해도 설정 탭으로 튕기는지 |
| **신호 탭** | 매수/매도 토글 미노출, **매수 에디션 단독 렌더**(D4: Play 빌드는 포트폴리오 표면이 없어 매도=상시 빈 탭) | 신호 탭에 매수/매도 토글이 없고 바로 에디션이 보이는지 |

**Pro 카피 정직화 — 전 채널(oci 포함, D1)**
| 화면 | 무엇이 바뀌었나 |
|---|---|
| **Pro 화면(`settings-detail/pro.tsx`)** | 혜택 4개 → 1개로 축소. '무제한 관심기업'(30종목 한도 없이) → **'관심기업 한도 확대 — 30종목→200종목'**(실구현 일치). 미구현 3혜택(고급 필터·우선 알림·심화 분석) 삭제 — 특히 '심화 분석'은 판단층 과금이라 재도입 금지(policy-non-advisory.md) |
| **설정 탭 Pro 배너 부제** | '무제한 관심기업 & 고급 필터' → **'관심기업 한도 확대 — 30 → 200종목'** |

**백엔드**: 무변경 (mobile/ 한정 UI 게이팅 + 카피 수정. 서버·M10 측정·watchlist 한도 로직 무접촉).

---

## 2026-07-17 (금) 배치 #11 — 빈 에디션 폴백 '오늘의 주요 공시' 브리핑 (DAR-552, 오너결정 A안)

**오너 결정 A안 end-to-end 완료**: 빈 날(매수판단 0, 실측 85%)에 그 KST 거래일의 주요 공시 브리핑을 채운다. 백엔드는 DAR-551(PR #531)로 선행 완료, 이번 반영은 모바일 소비 화면.

**모바일 (EAS 업데이트, 앱 재시작 2회 후 반영)**
| 화면 | 무엇이 바뀌었나 | 확인 방법 |
|---|---|---|
| **신호탭 · 매수 · 에디션(빈 날)** | 정직 4분기 카피("이 날은 주목할 신호가 없었어요" 등) **상위 유지** + 그 아래 **'오늘의 주요 공시 N건'** 섹션 신설. 각 행: 기업명·이벤트 라벨(공급계약 등)·요약 1줄. 탭 시 해당 공시 상세로 이동. 브리핑 대상일(PENDING/QUIET/COLD_START)만 노출 — 휴장(CLOSED)·미래(FUTURE)는 미노출(백엔드가 애초에 `fallbackBriefing` 미포함). 해당일 주요 공시가 없으면 섹션 자체 미노출(빈 배열) | ① 신호탭 → 매수 → 에디션 → 신호 없는 조용한 날짜(과거 날짜 플립) 진입 → ② 정직 카피 아래 '오늘의 주요 공시 N건' 섹션 확인 → ③ 아무 행 탭 → 해당 공시 상세로 이동 확인 |

> **정직 불변식**: 브리핑은 '판단'이 아니다 — 매수등급 카드와 다른 시각 언어(배지+한줄 요약)로 렌더하고, 정직 카피를 대체하지 않고 그 아래 별도 섹션으로만 이어 붙인다.

**백엔드**: 무변경 (mobile/ 한정 — DAR-551(PR #531)이 이미 제공한 `GET /signals/daily/:date` 응답 `meta.fallbackBriefing[]`을 소비만 함).

---

## 2026-07-17 (금) 배치 #10 — 에디션 알림 옵트인 배너(신호탭, DAR-547)

**모바일 (EAS 업데이트, 앱 재시작 2회 후 반영)**
| 화면 | 무엇이 바뀌었나 | 확인 방법 |
|---|---|---|
| **신호탭 · 매수 · 에디션** | **에디션 알림 옵트인 배너** — 에디션 푸시(DAR-523)는 기본 꺼짐 옵트인이라 발견성이 0이면 기능이 죽는다. 신호탭 에디션 뷰 상단에 '매일 저녁 7시, 그날의 투자판단 에디션' 배너를 띄워 **'알림 받기' 1탭으로 에디션 알림을 켜는** 동선을 제공. 닫기(X)는 SecureStore 로 영속돼 재노출하지 않고(재강요 금지·1회성), 이미 켠 사용자에겐 처음부터 안 보인다. | ① 신호 탭 → 매수 → '에디션' 모드 진입 → ② 날짜 스트립 위 배너 확인 → ③ '알림 받기' 탭 → '에디션 알림을 켰어요' 확인 다이얼로그 → 배너 사라짐(알림 설정의 '에디션 발행 알림' 토글도 켜짐) → ④ 다시 진입해도 배너 재노출 없음. (X로 닫아도 재노출 없음) |

**백엔드**: 무변경 (mobile/ 한정 — 기존 `/notification-settings` PATCH 계약의 `editionPushEnabled` 만 소비, 신규 API 없음).
## 2026-07-17 (금) 배치 #10 — 첫 게시(Play) 빌드 모의투자·자동매매 표면 전면 제외 (DAR-549, 오너 결정)

**오너 결정(2026-07-17)**: 처음 게시하는 앱 버전에서 모의투자·자동매매를 제외한다(정확도·기준·성과 신뢰 부족). UI 게이팅만 — 서버·M10 측정은 무접촉.

**구현 방식**: 빌드타임 플래그 `EXPO_PUBLIC_SHOW_TRADING`(기본 `true`) + `eas.json`에 `play` 프로파일 신설(channel `play`, `SHOW_TRADING=false`). **기존 oci/preview/production 채널은 무변경**(플래그 미설정 → 기존과 100% 동일, 회귀 0). 아래 변화는 **새 `play` 빌드에서만** 나타난다.

**모바일 — `play` 빌드에서만 (현행 oci 빌드는 변화 없음)**
| 화면 | 무엇이 바뀌었나 | 확인 방법 (play 빌드 기준) |
|---|---|---|
| **하단 탭 IA** | 포트폴리오 탭 숨김 → **5탭에서 4탭으로**(홈·알림·신호·설정) | 앱 하단 탭바에 '포트폴리오'가 없는지 |
| **홈** | '운용 성과' footer(졸업 트래커·전환 현황) **미렌더** | 홈 피드 최하단에 '운용 성과' 카드가 없는지 |
| **트레이딩 화면(자동매매·체결내역·백테스트·전략)** | 라우트·딥링크 진입 시 **홈으로 리다이렉트** | 구 링크/딥링크로 진입해도 홈으로 튕기는지 |
| **신호 카드** | 카드 자체는 유지, '모의매매' 연결 진입점 없음(현재도 부재 — 가드로 고정) | 신호 카드에 모의매매 버튼이 없는지 |

**백엔드**: 무변경 (mobile/ 한정 UI 게이팅. 서버·측정 파이프라인 무접촉).

---

## 2026-07-17 (금) 배치 #9 — 증권사 앱 핸드오프('증권사 앱에서 열기', DAR-545)

**모바일 (EAS 업데이트, 앱 재시작 2회 후 반영)**
| 화면 | 무엇이 바뀌었나 | 확인 방법 |
|---|---|---|
| **신호 상세 · 종목 상세** | **증권사 앱 핸드오프 v1** — 매매 불가 앱의 '판단 후 실행' 공백을 대표 증권사 앱(토스증권·키움 영웅문S#·삼성 mPOP) 링크아웃으로 메움. 설치돼 있으면 앱 실행, 없으면 스토어로 폴백. 특정사 추천 아님·계좌/주문 연동 아님(정직 고지 시트). | ① 신호 상세 또는 종목 차트 화면에서 '증권사 앱에서 열기' 탭 → ② 시트에서 증권사 선택 → ③ 설치돼 있으면 해당 앱 실행 / 미설치면 앱스토어로 이동. ④ 시트 하단 고지('추천 아님·미설치 시 스토어 안내')가 보이는지 확인 |

> **머지 전 필수 수동 QA(헤드리스 불가)**: 실기기(iOS·Android) 3사 스킴 오픈/스토어 폴백 실동작 검증 — `appScheme`·스토어 URL 은 공개 등록정보 기준값이라 기기별 실동작 확인이 필요합니다(스킴 오차 시 스토어 폴백이 흐름을 보증하나, '앱 열림' 경로 자체는 기기 검증으로 확정). de-risk 완료: `canOpenURL` 미사용(iOS `LSApplicationQueriesSchemes` 네이티브 변경 회피)·`openURL` 시도→실패 캐치 폴백이라 JS-only·양 플랫폼 동일 경로.

**백엔드**: 무변경 (mobile/ 한정, 서버 API 미소비 — 순수 링크아웃).

---

## 2026-07-17 (금) 배치 #8 — 예정 이벤트 캘린더 화면

**모바일 (EAS 업데이트, 앱 재시작 2회 후 반영)**
| 화면 | 무엇이 바뀌었나 | 확인 방법 |
|---|---|---|
| **홈 + 전용 화면 (신규)** | **예정 이벤트 캘린더** — 관심종목의 보호예수 해제·청약일 등 D-day 목록(홈 섹션 + 전체 화면), 이벤트 탭 시 해당 공시 상세로 | 홈 하단 '예정 이벤트' 섹션 → 전체 보기 |

**백엔드**
- 내부자 쿼터 경로 영속 카운터 경유 판정 완료(이미 커버 — 회귀 테스트 고정)

---

## 2026-07-17 (금) 배치 #7 — 예정 이벤트 캘린더 API + 안정화

**백엔드 (즉시 반영)**
- **공시발 예정 이벤트 캘린더 v1(API)** — 보호예수 해제·청약일 등 미래 이벤트를 공시에서 구조화 추출(불확실 날짜 미표시 정직 규약, 마이그레이션 0). *FE 화면 연동은 차기 배치*
- 야간 쿼터 포렌식 판정 **SUPPORTED** — 재기동 11회×예산 재개방으로 62,928콜(상한 4.5배) 실증. 어제의 영속화 수정이 근본 해결책임을 데이터로 확인, 내부자 경로 커버리지 후속(DAR-540) 진행
- 모의운용 통합 스펙 flake 격리(측정 무영향)

---

## 2026-07-17 (금) 배치 #6 — 콜드스타트 온보딩 + 쿼터 포렌식

**모바일 (EAS 업데이트, 앱 재시작 2회 후 반영)**
| 화면 | 무엇이 바뀌었나 | 확인 방법 |
|---|---|---|
| 홈 (관심기업 0 상태) | **콜드스타트 온보딩** — 첫 로그인 후 관심기업이 없으면 인기/활발 종목에서 3~5개 선택 유도(건너뛰기 가능, 코치마크 승격형) | 관심기업 전부 삭제 후 홈 진입 |

**백엔드**
- DART 쿼터 야간 소진 포렌식 진단 엔드포인트(/ops/dart-quota-forensics) — 읽기 전용, PM이 소비 경로 정량 판정에 사용

---

## 2026-07-17 (금) 배치 #5 — 재무맥락 렌더 + 구조 정리 + 쿼터 하드보장

**모바일 (EAS 업데이트, 앱 재시작 2회 후 반영)**
| 화면 | 무엇이 바뀌었나 | 확인 방법 |
|---|---|---|
| '왜 움직였나' 카드 | **재무 맥락 한 줄 렌더** — "이번 수주는 작년 매출의 N%" (분모 불확실 시 행 자체 없음) | PRICE_MOVE 푸시 → 카드 중단 |
| 신호탭 | 구 14일창 큐레이션 잔재 완전 폐기·이중 점수정렬 제거 — 전 화면이 에디션 축으로 통일 (시각적 변화는 미미, 정합성 개선) | — |

**백엔드 (즉시 반영, 마이그레이션 1건)**
- DART 쿼터 상태 **재기동 영속화**(dart_quota_state) — 재기동 시 라이브 예약분 소실로 인한 수집 기아 재발 하드 차단

---

## 2026-07-17 (금) 배치 #4 — Wave B/C 후속 + P0 수정

**모바일 (EAS 업데이트, 앱 재시작 2회 후 반영)**
| 화면 | 무엇이 바뀌었나 | 확인 방법 |
|---|---|---|
| 신호탭 | **'놓친 호' 미읽음 도트** — 마지막으로 본 에디션 이후 새 호가 나오면 날짜 칩에 도트 표시, 열람 시 소거 | 신호탭 → 날짜 스트립의 미열람 날짜 칩에 도트 |
| (푸시 수신 시) | 에디션 발행 푸시 탭 → **해당 호로 직행** (딥링크) | 19:05 에디션 푸시 수신 후 탭 |
| '왜 움직였나' 카드 | **재무 맥락 한 줄** — "이번 수주는 작년 매출의 N%" (분모 불확실하면 행 자체 미표시) | 급변동(PRICE_MOVE) 푸시 → 카드 진입 *(FE 렌더는 DAR-534 진행 중 — 다음 배치)* |

**백엔드 (즉시 반영)**
- **P0 수정**: 단타 트랙 개장직후 유령 분봉 진입 봉인(수집 거부+미래봉 하드가드) — 재발 차단. *오늘자 오염 5건 정리는 오너 승인 대기*
- 공시 수집 최근 갭 자동 복구 + DART 쿼터 관측성 (DAR-434 해묵은 결함 종결)
- auto-kill 경보 10분 스팸 → 하루 1회
- 에디션 푸시 딥링크가 해당 호 날짜로 직행하도록 페이로드 재타겟

---

## 2026-07-17 (금) 배치 #3 — '왜 움직였나' + 푸시 한 줄 판단

**모바일 (EAS)**
| 화면 | 무엇이 바뀌었나 | 확인 방법 |
|---|---|---|
| **'왜 움직였나' 카드 (신규 화면)** | 급변동 원인 3상태 정직 분기: 원인 공시 요약+유사공시 통계 / "최근 48시간 관련 공시 없음" / 실패. 면책 고정 | PRICE_MOVE 푸시 탭 → `/price-move/[refId]` 진입 |

**백엔드**
- PRICE_MOVE ±5% 발화 시 **AI 원인 역추적**(48h 내 공시 존재 시만, 무공시면 AI 호출 0, 일 $0.5 상한)
- 푸시 본문 '한 줄 판단' 표준 — "수주 1,200억 — 유사공시 D+5 평균 +2.1% (n=142)" 형식(에디션 푸시부터, n<30이면 통계 생략)

---

## 2026-07-17 (금) 배치 #2 — Wave A: 통계·계측·알림 거버넌스

**모바일 (EAS)**
| 화면 | 무엇이 바뀌었나 | 확인 방법 |
|---|---|---|
| **공시 상세** | **'과거 유사공시 반응' 섹션 신설** — 이 유형 공시 N건의 D+5 평균수익률·승률·표본 n. n<30이면 "표본이 부족해 통계를 표시하지 않습니다" | 아무 공시 상세 열기 → 중단 통계 섹션 |
| 홈 | **저장공시 → '보관함' 명명 승격**, 홈 상단 점유 축소(피드·에디션이 상위 위계) | 홈 스크롤 시 위계 확인 |
| 설정 → 알림 | **알림 설정 센터 v2** — 계열별(공시/급변동/매수매도/에디션/다이제스트) 개별 on/off + 일일 푸시 상한. 에디션·다이제스트는 **기본 OFF**(원하면 켜기) | 설정 → 알림 설정 |

**백엔드**
- 유사공시 반응 통계 API(n≥30 정직 게이트) · 에디션 밀도 진단 · freshness 제로런 감지(휴장일 오탐 0) · 테스터 행동 계측(PII 무수집) · **에디션 발행 푸시 19:05 크론**(빈 에디션 발송 금지, 기본 OFF 옵트인)

---

## 2026-07-17 (금) 배치 #1 — 일일 투자판단 에디션 (대개편)

**모바일 (EAS + 신규 APK)**
| 화면 | 무엇이 바뀌었나 | 확인 방법 |
|---|---|---|
| **홈** | "오늘의 투자판단" **거짓 '오늘' 표기 제거** → 날짜 인지 헤더("오늘의 투자판단" / "최신 투자판단 · M/D · N일 전") + **최신 에디션 요약 카드**(빈 날이면 사유 카피: 휴장/생성 전/조용/축적 전) | 홈 첫 카드 — 헤더에 날짜가 붙는지, 오늘 판단 없으면 정직 카피 |
| **신호탭 (대개편)** | **일일 에디션 브라우징** — 상단 고정 날짜 스트립(오늘/어제/MM.DD·건수 dot)으로 신문 넘기듯 하루씩. 과거 호는 "지난 판단 · 현재 시세와 다를 수 있어요" 배너+흐림 처리 | 신호탭 → 날짜 칩 좌우 탐색 |
| 신호 카드 전체 | **절대날짜(M/D) 상시 배지** — 'N시간 전' 단독 표기 폐기, 만료/지연 상태 표기 | 아무 신호 카드의 날짜 배지 |
| 게스트 홈 | 로그인 유도 카피 "오늘의 투자판단" → "**최신** 투자판단" 정직화 | 로그아웃 상태 홈 |

**백엔드**
- 에디션 조회 API 2종(`daily-editions`·`daily/:date`) — KST 거래일 귀속·백필 제외·빈 날 4분기 사유
- (버그 수정) 에디션 API SQL 컬럼 오류 — 출시 직후 플릿 자가 발견·즉일 수정

---

## 대기 중 (오너 판정 필요)
1. **오늘자 오염 정리**: 7/17 유령 분봉·유령 진입 5건 정정(M10 측정 무결성 복원) — "정리해" 승인 시 집행

## 완료 (오너 판정 반영됨)
1. **에디션 빈 날 폴백**: 60거래일 실측 결과 빈 날 85% → **A안(당일 주요 공시 브리핑) 채택**, 백엔드 DAR-551(PR #531)·모바일 DAR-552(배치 #11)로 end-to-end 완료

*갱신: 2026-07-17 · 담당: PM(Claude) — 반영마다 이 문서 최상단에 추가*
